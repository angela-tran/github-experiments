name: bash

on:
  workflow_dispatch:


jobs:

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Run a multi-line script
        run: |
          hi="hi"
          test="testing"
          echo $hi $test
          replaced=${test/test/th}
